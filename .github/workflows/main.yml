name: Deploy boldi

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Deploy over SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.DB_HOST }}
          username: ${{ secrets.DB_USER }}
          key: ${{ secrets.SSH_KEY }}
          script: |
            echo "🔹 Updating server packages"
            sudo apt-get update -y

            echo "🔹 Installing Node.js & npm"
            curl -fsSL https://deb.nodesource.com/setup_22.x | sudo -E bash -
            sudo apt-get install -y nodejs

            echo "🔹 Installing Docker Compose"
            sudo apt-get install -y docker-compose

            echo "🔹 Creating project folder if it doesn't exist"
            mkdir -p /home/ubuntu/Shifo_Yoli_Backend
            cd /home/ubuntu/Shifo_Yoli_Backend

            if [ ! -d .git ]; then
              echo "🔹 Cloning repository"
              git clone https://github.com/Asqaraliyev-Faxriddin/Shifo_Yoli_Backend .
            else
              echo "🔹 Pulling latest changes"
              git pull origin main
            fi

            echo "🔹 Installing dependencies"
            npm install

            echo "🔹 Downloading wait-for-it.sh"
            curl -o wait-for-it.sh https://raw.githubusercontent.com/vishnubob/wait-for-it/master/wait-for-it.sh
            chmod +x wait-for-it.sh

            echo "🔹 Stopping running containers"
            docker-compose down

            echo "🔹 Building containers"
            docker-compose build

            echo "🔹 Starting containers with wait-for-it"
            docker-compose run --rm app sh -c "
              ./wait-for-it.sh db:5432 --timeout=30 --strict -- npx prisma generate &&
              ./wait-for-it.sh db:5432 --timeout=30 --strict -- npx prisma migrate deploy
            "

            echo "🔹 Running containers in detached mode"
            docker-compose up -d
